import sympy
n = 3
a = [[0] * n for i in range(n)]
for i in range(n):
    for j in range(n):
        if i < j:
            a[i][j] = sympy.randprime(0,100)
        elif i > j:
            a[i][j] = sympy.randprime(0,100)
        else:
            a[i][j] = sympy.randprime(0,100)
for row in a:
    print(' '.join([str(elem) for elem in row]))
